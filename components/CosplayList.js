import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default CosplayList = ({ cosplayList }) => {
  return (
    <View>
      <Text>{cosplayList.cosplay}</Text>
    </View>
  )
}